# Getting Started

This is a placeholder page — replace it with real steps for **your product's**
end users (not developers): how to log in, where the main screens are, and what
to do first.

## Suggested structure for a real page

- **What you'll see** — a screenshot of the screen this page documents.
- **What it's for** — one or two sentences, plain language.
- **How to use it** — numbered steps for the common tasks on this screen.
- **Related pages** — links to other guide pages that connect to this one.

See the `user-guide-builder` skill for the full pipeline: audience-audit → boot the
real app → logged-in tour for per-item screenshots → mine epics into a domain
digest → grounded per-page writers → verify anchors → redeploy.
