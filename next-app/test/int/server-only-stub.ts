// Empty stub for the `server-only` package under vitest (it has no runtime impl
// outside the Next.js bundler). Aliased in vitest.int.config.ts so lib modules
// that import "server-only" load fine under plain vitest node.
export {}
