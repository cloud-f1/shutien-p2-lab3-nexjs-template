# Workflow disabled 2026-05-20 — 手動觸發才會執行
# 恢復方式：還原 on: push branches: [main] + pull_request branches: [main]
name: CI

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Trigger manually only."
