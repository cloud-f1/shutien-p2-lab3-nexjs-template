# Workflow disabled 2026-05-20 — 手動觸發才會執行
# 恢復方式：還原 on: pull_request types: [opened, synchronize, reopened] + pull_request_target types: [closed]
name: Preview Deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Trigger manually only."
