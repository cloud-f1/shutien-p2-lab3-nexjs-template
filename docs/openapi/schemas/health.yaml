HealthResponse:
  type: object
  required: [status]
  properties:
    status:
      type: string
      enum: [ok, healthy, degraded]
    version:
      type: string
      example: 0.1.0
    database:
      type: string
      enum: [connected, disconnected]
