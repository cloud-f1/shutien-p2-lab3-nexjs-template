# Promotion Proposal — 20260519-004532

Auto-generated by `scripts/hooks/auto-promote-check.sh` after threshold of 4 pending `[GENERALIZABLE]` lessons.

## Next step

`/athena:promote --apply docs/context/promotion-proposals/20260519-004532.md`

## Pending lessons

docs/context/qa-patterns.md:33:- [GENERALIZABLE] Two-layer UUID design: models use TypeDecorator (GUID), migrations use sa.Uuid(). Autogenerate hooks bridge the gap. (E62, E63)
docs/context/qa-patterns.md:34:- [GENERALIZABLE] CSP whitelisting for third-party integrations must be explicit per directive. (E57)
docs/context/qa-patterns.md:38:- [GENERALIZABLE] SQLite masks schema bugs. Test migrations against PostgreSQL via Docker tmpfs. (E65)
docs/context/qa-patterns.md:39:- [GENERALIZABLE] Migration linter as pytest: scan for banned patterns, runs in 0.02s. (E62)
docs/context/qa-patterns.md:43:- [GENERALIZABLE] SHA-pin all GitHub Actions. Tags are mutable. (E61)
docs/context/qa-patterns.md:46:- [GENERALIZABLE] AI skill documents prevent anti-pattern propagation. (E64)
docs/context/qa-patterns.md:52:- [GENERALIZABLE] Provider/strategy pattern with factory singleton for swappable integrations (email, SMS, storage). Leverage transitive deps before adding new ones. (E66)
docs/context/qa-patterns.md:53:- [GENERALIZABLE] Zero-domain must be valid: framework core starts, builds, and passes tests with no domain modules installed. (E70)
docs/context/qa-patterns.md:54:- [GENERALIZABLE] After a refactor, cascade config changes through env templates, deploy scripts, and docker-compose in dependency order. (E66→E67→E68)
docs/context/qa-patterns.md:57:- [GENERALIZABLE] Template repos need a post-clone reset script: safety-guarded (branch count), idempotent, with stored template-ready state as source of truth. (E69, E71)
docs/context/qa-patterns.md:58:- [GENERALIZABLE] Separate framework from examples: move domain implementations to docs/examples/, provide an installer script to add them back selectively. (E70)
docs/context/qa-patterns.md:63:- [GENERALIZABLE] Observability as hook-only: webhook + JSONL audit log via hook scripts — zero app code changes, env-var gated (`$AI_CODING_WEBHOOK_URL`), fire-and-forget. (E82)
docs/context/qa-patterns.md:64:- [GENERALIZABLE] Dependency graph as standalone script (not inline in command): enables reuse by multiple consumers (`/athena:batch`, `@orchestrator`, CI). Kahn's algorithm for topological sort → wave partitioning. (E84)
docs/context/qa-patterns.md:65:- [GENERALIZABLE] Agent coordination pattern: dedicated orchestrator agent that CANNOT write code — only reads graph, dispatches worktree agents, handles retries. Separation of concerns prevents scope creep. (E86)
docs/context/qa-patterns.md:68:- [GENERALIZABLE] Stop verifier as extensible quality gate: each rule is a self-contained block (blocking exit 2 or warning). Adding rules requires only appending to the script — no framework changes. Per-file vs global scope distinction. (E83)
docs/context/qa-patterns.md:69:- [GENERALIZABLE] Bash 3.2 compatibility matters on macOS: no associative arrays (`declare -A`), use `awk` for all graph operations. Tested with `bash -n` syntax validation. (E84)
docs/context/qa-patterns.md:75:- [GENERALIZABLE] Bilingual guides (EN + ZH-TW) for every major feature addition. New capability → new guide entry + README section + CLAUDE.md update — three-file documentation pattern. (E86)
docs/context/qa-patterns.md:80:- [GENERALIZABLE] Superuser-only admin endpoints: use `current_superuser` dependency (from fastapi-users), return structured JSON with DB latency, provider status, app version, uptime. Separate from public `GET /health`. (E105)
docs/context/qa-patterns.md:81:- [GENERALIZABLE] Production fail-fast pattern: `validate_production_config()` in lifespan — check secrets not placeholder, DB not SQLite, before `yield`. Collect ALL violations into one error message. Dev/test environments skip validation. (E104)
docs/context/qa-patterns.md:84:- [GENERALIZABLE] Contract tests for admin endpoints: validate response against OpenAPI schema + test superuser access (200), non-superuser rejection (403), unauthenticated (401). Three-assertion pattern for auth-gated endpoints. (E105)
docs/context/qa-patterns.md:87:- [GENERALIZABLE] Post-clone verification script (`make verify`): 8 checks for placeholder replacement, OAuth config, email provider, customized project name. Exit 1 on failure for CI integration. Complements `make doctor` (tool presence) with content validation. (E106)
docs/context/qa-patterns.md:88:- [GENERALIZABLE] Production readiness script (`make doctor-production`): separate from dev `make doctor`. Checks secrets, DB type, email provider, OAuth — exit 1 for CI. Two-tier doctor pattern: dev checks tools, production checks config values. (E104)
docs/context/qa-patterns.md:91:- [GENERALIZABLE] Agent write-back docs need periodic hygiene sweeps: test-status.md drifted from 28→259 tests over 20 phases. Schedule context doc audits every 5-10 phases. Add verified-date stamps to catch drift. (E102)
docs/context/qa-patterns.md:97:- [GENERALIZABLE] **In-process spec↔server conformance.** Load the OpenAPI
docs/context/qa-patterns.md:106:- [GENERALIZABLE] **xfail list, not spec freeze.** When first wiring the
docs/context/qa-patterns.md:112:- [GENERALIZABLE] **Exclude framework-level checks.** Schemathesis ships
docs/context/qa-patterns.md:167:- [GENERALIZABLE] Two-file Docker Compose split: `docker-compose.yml` (dev, source mounts, hot-reload) + `docker-compose.prod.yml` (build images, nginx, production env). Dev uses base images + source mounts; prod builds from multi-stage Dockerfiles. Clear separation prevents accidental production with dev settings. (E107, E108)
docs/context/qa-patterns.md:168:- [GENERALIZABLE] Named volumes for `node_modules` in Docker dev mode (`client_node_modules:/app/node_modules`) prevent host/container platform conflicts (native binaries like esbuild differ between macOS and Linux). (E107)
docs/context/qa-patterns.md:169:- [GENERALIZABLE] Production compose uses `${VAR:-default}` syntax for ALL env vars — allows override from host `.env` file or shell exports without editing the compose file. Dev compose hardcodes safe defaults. (E108)
docs/context/qa-patterns.md:172:- [GENERALIZABLE] Make targets as thin wrappers over Docker Compose: `docker-dev`, `docker-prod`, `docker-down`, `docker-logs`, `docker-ps`. URL info printed BEFORE blocking `docker compose up` call (echo after `up` never executes until Ctrl+C). (E109)
docs/context/qa-patterns.md:173:- [GENERALIZABLE] Operational scripts pair: `docker-clean.sh` (system prune with confirmation + optional volume prune with extra warning) + `db-backup.sh` (pg_dump to timestamped .sql.gz via Docker exec, prints restore command). Both are no-op safe when containers not running. (E111)
docs/context/qa-patterns.md:174:- [GENERALIZABLE] Pre-deploy gate layering: `make doctor` (tool presence) → `make verify` (placeholder replacement) → `make doctor-production` (config values: alembic check, DEBUG=false, no-localhost origins, secret length, client build). Three tiers of validation for three audiences. (E110)
docs/context/qa-patterns.md:179:- [GENERALIZABLE] **Iterative review convergence loop (RDT-inspired).**
docs/context/qa-patterns.md:192:- [GENERALIZABLE] **Idempotent agent + round mode.** `@reviewer` becomes
docs/context/qa-patterns.md:198:- [GENERALIZABLE] **Stuck detection via content hash, not item count.**
docs/context/qa-patterns.md:215:- [GENERALIZABLE] **Worktree isolation needs a TWO-LAYER probe, not one.**
docs/context/qa-patterns.md:232:- [GENERALIZABLE] **Inline orchestrator steps don't fire TaskCompleted
docs/context/qa-patterns.md:250:- [GENERALIZABLE] **`gh pr merge --auto` "fast-forward" warning is
docs/context/qa-patterns.md:262:- [GENERALIZABLE] **Read-only contract verified by SHA256 snapshot is
