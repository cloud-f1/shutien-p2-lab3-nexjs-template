## {{PASCAL}} domain — OpenAPI path definitions

/{{KEBAB_PLURAL}}:
  get:
    tags:
      - {{KEBAB_PLURAL}}
    summary: List {{SNAKE_PLURAL}}
    operationId: list_{{SNAKE_PLURAL}}
    security:
      - BearerAuth: []
    parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
          minimum: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 20
          minimum: 1
          maximum: 100
    responses:
      "200":
        description: Paginated list of {{SNAKE_PLURAL}}
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Paginated{{PASCAL}}Response"
      "401":
        description: Unauthorized

  post:
    tags:
      - {{KEBAB_PLURAL}}
    summary: Create {{SNAKE}}
    operationId: create_{{SNAKE}}
    security:
      - BearerAuth: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/{{PASCAL}}Create"
    responses:
      "201":
        description: Created
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/{{PASCAL}}Read"
      "401":
        description: Unauthorized
      "422":
        description: Validation error

/{{KEBAB_PLURAL}}/{{{SNAKE}}_id}:
  get:
    tags:
      - {{KEBAB_PLURAL}}
    summary: Get {{SNAKE}} by ID
    operationId: get_{{SNAKE}}
    security:
      - BearerAuth: []
    parameters:
      - name: {{SNAKE}}_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    responses:
      "200":
        description: {{PASCAL}} detail
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/{{PASCAL}}Read"
      "401":
        description: Unauthorized
      "404":
        description: Not found

  patch:
    tags:
      - {{KEBAB_PLURAL}}
    summary: Update {{SNAKE}}
    operationId: update_{{SNAKE}}
    security:
      - BearerAuth: []
    parameters:
      - name: {{SNAKE}}_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/{{PASCAL}}Update"
    responses:
      "200":
        description: Updated
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/{{PASCAL}}Read"
      "401":
        description: Unauthorized
      "404":
        description: Not found
      "422":
        description: Validation error

  delete:
    tags:
      - {{KEBAB_PLURAL}}
    summary: Delete {{SNAKE}}
    operationId: delete_{{SNAKE}}
    security:
      - BearerAuth: []
    parameters:
      - name: {{SNAKE}}_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    responses:
      "204":
        description: Deleted
      "401":
        description: Unauthorized
      "404":
        description: Not found
