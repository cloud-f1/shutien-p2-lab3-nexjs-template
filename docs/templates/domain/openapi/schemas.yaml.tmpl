## {{PASCAL}} domain — OpenAPI schema definitions

{{PASCAL}}Create:
  type: object
  required:
    {{#FIELDS_REQUIRED}}
    - {{field_name}}
    {{/FIELDS_REQUIRED}}
  properties:
    {{#FIELDS}}
    {{field_name}}:
      type: {{openapi_type}}
      {{#openapi_format}}
      format: {{openapi_format}}
      {{/openapi_format}}
      {{#max_length}}
      maxLength: {{max_length}}
      {{/max_length}}
    {{/FIELDS}}

{{PASCAL}}Read:
  type: object
  required:
    - id
    {{#FIELDS_REQUIRED}}
    - {{field_name}}
    {{/FIELDS_REQUIRED}}
    - user_id
    - created_at
    - updated_at
  properties:
    id:
      type: string
      format: uuid
    {{#FIELDS}}
    {{field_name}}:
      type: {{openapi_type}}
      {{#openapi_format}}
      format: {{openapi_format}}
      {{/openapi_format}}
    {{/FIELDS}}
    user_id:
      type: string
      format: uuid
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time

{{PASCAL}}Update:
  type: object
  properties:
    {{#FIELDS}}
    {{field_name}}:
      type: {{openapi_type}}
      {{#openapi_format}}
      format: {{openapi_format}}
      {{/openapi_format}}
      {{#max_length}}
      maxLength: {{max_length}}
      {{/max_length}}
    {{/FIELDS}}

Paginated{{PASCAL}}Response:
  type: object
  required:
    - items
    - total
    - page
    - page_size
    - pages
  properties:
    items:
      type: array
      items:
        $ref: "#/components/schemas/{{PASCAL}}Read"
    total:
      type: integer
    page:
      type: integer
    page_size:
      type: integer
    pages:
      type: integer
