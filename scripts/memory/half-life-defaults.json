{
  "$comment": "E185 — Half-life defaults per Tier 0 file. The category IS the half-life (no separate rubric). Used by @memory-curator on promotion and by E181 score.sh decay calculation. Resolution order: file frontmatter > this map by filename > 180-day fallback.",
  "$schema_version": 1,
  "defaults": {
    "architecture-lessons.md": 365,
    "anti-patterns.md": 365,
    "security-learnings.md": 365,
    "failure-patterns.md": 30,
    "integration-gotchas.md": 90,
    "testing-patterns.md": 180,
    "performance-insights.md": 180,
    "NEW_PROJECT_PRIMER.md": 365,
    "architecture-patterns.md": 180,
    "design-handoff-pattern.md": 180,
    "dx-patterns.md": 180,
    "mockup-contract.md": 180,
    "workflow-patterns.md": 180
  },
  "fallback_days": 180,
  "rationale": {
    "architecture-lessons.md": "Decisions that proved durable; survive stack changes",
    "anti-patterns.md": "Things never to do; reasons rarely expire",
    "security-learnings.md": "Vulnerabilities + their fixes — high cost if forgotten",
    "failure-patterns.md": "Specific bug + fix — once internalized, can fade",
    "integration-gotchas.md": "Third-party surprises — relevant until vendor changes API",
    "testing-patterns.md": "Test approaches — durable but tied to current stack",
    "performance-insights.md": "Performance learnings — tied to current infrastructure",
    "NEW_PROJECT_PRIMER.md": "The curated digest — load-bearing, always-on",
    "architecture-patterns.md": "E363 — backfilled (was missing from this registry while present in lesson-tags.json; not evergreen, tied to current stack, matches architecture-lessons.md's sibling category)",
    "design-handoff-pattern.md": "E363 — backfilled; design/UI gotchas tied to the current design system",
    "dx-patterns.md": "E363 — backfilled (was missing from this registry despite being an actual promoted Tier 0 file — the E363 root-cause bug); matches its own promoted frontmatter (half_life_days: 180)",
    "mockup-contract.md": "E363 — backfilled; design/spec gotchas tied to the current mockup pipeline",
    "workflow-patterns.md": "E363 — backfilled (was missing from this registry despite being an actual promoted Tier 0 file — the E363 root-cause bug); matches its own promoted frontmatter (half_life_days: 180)"
  }
}
